# Web3Auth Configuration

# Server Configuration
PORT=4000
NODE_ENV=development

# Cryptographic Keys (Generate secure random values)
# SALT should be a random string for hashing
SALT=your_random_salt_here

# AES-256-CBC encryption key (32 bytes in hex format)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
KEY=your_32_byte_hex_key_here

# AES-256-CBC initialization vector (16 bytes in hex format)
# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
IV=your_16_byte_hex_iv_here

# Solana Configuration
# Cluster URL (mainnet-beta, testnet, devnet, or custom RPC URL)
CLUSTER=https://api.devnet.solana.com

# Minimum balance required in LAMPORTS (0 to disable balance check)
MIN_BALANCE=1000000

# Security Configuration
# Comma-separated list of allowed origins for CORS (use * for all origins in development)
ALLOWED_ORIGINS=http://localhost:4000,http://localhost:4001

# Example values for development (DO NOT use in production):
# SALT=dev_salt_12345
# KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
# IV=0123456789abcdef0123456789abcdef